import { app } from './app';

const start = async () => { 
  
    try {     
        
      } catch (err) {
        console.error(err);
      }

  app.listen(7000, () => {
    console.log('Listening on port 7000!!!!!!!!');
  });
};

start();